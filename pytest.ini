[pytest]
# IAM local+aws siempre. Smoke local: skip si LocalStack down. Smoke aws: skip sin credenciales.
testpaths = tests
pythonpath = .
markers =
    aws: necesita profile poc-aws y stack iac/aws aplicado
