# `tests/` — pruebas del proyecto

Agrupadas por stack (ADR 002). Se ejecutan **en el devcontainer**.

- **`local/`** — pytest contra políticas IAM y, si LocalStack está up, smoke en `:4566`
- **`aws/`** — etapa 2 (todavía no)
